import React, {
  PureComponent
} from 'react';
import ReactDOM from 'react-dom';

export default class EditPost extends PureComponent {

  render() {
    return (
      <div>
        //form
      </div>
    );
  }
}
